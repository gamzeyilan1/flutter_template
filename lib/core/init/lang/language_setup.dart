import 'package:flutter/material.dart';
class LanguageSetup {

  static const enLocale = Locale("en");
  static const trLocale = Locale("tr");

  static const List<Locale> supportedLocales = [enLocale, trLocale];

}